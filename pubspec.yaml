name: penanam_pintar_v6
description: Aplikasi Android untuk pemupukan tanaman cerdas (AI + Google Drive)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  google_sign_in: ^6.2.0
  googleapis: any
  googleapis_auth: any

flutter:
  uses-material-design: true
  assets:
    - assets/
